import { styled, Box } from '@mui/material';

export const Container = styled(Box)({
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    minHeight: '100vh'
}) as typeof Box